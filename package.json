{
  "name": "habitstreak",
  "version": "1.0.0",
  "description": "Habit Streak - Daily Habit Tracker",
  "scripts": {
    "sync": "npx cap sync android",
    "build:android": "npx cap sync android && cd android && ./gradlew assembleRelease",
    "build:bundle": "npx cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2",
    "@capacitor/splash-screen": "^6.0.2",
    "@capacitor/local-notifications": "^6.1.0",
    "@capacitor-community/admob": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
